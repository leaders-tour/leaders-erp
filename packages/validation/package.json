{
  "name": "@tour/validation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "zod": "^3.23.8",
    "@tour/domain": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'",
    "test": "echo 'no tests for @tour/validation'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
