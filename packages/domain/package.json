{
  "name": "@tour/domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint 'src/**/*.ts'",
    "test": "echo 'no tests for @tour/domain'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
