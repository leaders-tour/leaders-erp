type TimeBlock {
  id: ID!
  locationId: String!
  startTime: String!
  label: String!
  orderIndex: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
  location: Location!
  activities: [Activity!]!
}

input TimeBlockCreateInput {
  locationId: String!
  startTime: String!
  label: String!
  orderIndex: Int!
}

input TimeBlockUpdateInput {
  locationId: String
  startTime: String
  label: String
  orderIndex: Int
}
